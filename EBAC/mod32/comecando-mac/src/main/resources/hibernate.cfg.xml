<property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/hibernate_db</property>
        
        <property name="hibernate.connection.username">raporto</property>
        <property name="hibernate.connection.password">rpbpp3009</property>
        
        
        ```

### 🚀 Último Passo: Testar!

Com o banco de dados criado e o `hibernate.cfg.xml` ajustado, você está pronto para rodar a classe **`MainApp.java`** no VS Code.

Me avise se o comando de conexão (`/opt/homebrew/.../psql postgres`) funcionou e se você conseguiu recriar seu usuário e banco de dados. Se sim, **vá em frente e teste seu código Java!**